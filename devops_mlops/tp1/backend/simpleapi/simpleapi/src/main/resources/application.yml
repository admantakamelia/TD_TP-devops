spring:
  datasource:
    url: jdbc:postgresql://my-postgres-init:5432/db
    username: usr
    password: pwd
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
